type ImportExportHistory{
    id:ID
    uid:String
    dateCreated:String
    lastUpdated:String
    sessionType:String
    object:String
    status:String
    count:Int
    userName:String
    startTime:String
    finishTime:String
}

extend type Query{
    getImportExportHistoryById(id:ID) : ImportExportHistory

    ImportExportHistory(where:String, sort:String) : [ImportExportHistory]!
}

extend type Mutation{
    
}