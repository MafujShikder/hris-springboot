type MessageThread{
    id:ID
    uid:String
    dateCreated:String
    lastUpdated:String
    createdBy:Int
    subject:String
    createDate:String
    isSpam:String
    messageThreadMetadata:MessageThreadMetadata
}

extend type Query{
    getMessageThreadById(id:ID!) : MessageThread

    MessageThread(where:String, sort:String) : [MessageThread]!
}

extend type Mutation{
    
}