type HelpChapter{
    id:ID
    uid:String
    dateCreated:String
    lastUpdated:String
    name:String
    description:String
    content:String
    helpTopics:[HelpTopic]
}

extend type Query{
    getHelpChapterById(id:ID):HelpChapter

    HelpChapter(where:String, sort:String) : [HelpChapter]!
}

extend type Mutation{
    
}