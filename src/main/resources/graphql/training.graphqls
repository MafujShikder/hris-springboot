type Training{
    id:ID
    dateCreated:String
    lastUpdated:String
    courseName:String
    trainingCategory:String
    trainingInstruction:String
    curiculum:String
    trainingInstance:TrainingInstance
}

extend type Query{
    getTrainingById(id:ID!):Training

    Training(where:String, sort:String) : [Training]!
}

extend type Mutation{
    
}