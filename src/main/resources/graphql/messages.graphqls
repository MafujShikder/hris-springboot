type Message{
    id:ID
    uid:String
    dateCreated:String
    lastUpdated:String
    messageThread : MessageThread
    user:User
    body:String
    createdAt:String
    messageMetadata: MessageMetadata
}

extend type Query{
    getMessageById(id:ID):Message

    Message(where:String, sort:String) :[Message]!
}

extend type Mutation{
    
}