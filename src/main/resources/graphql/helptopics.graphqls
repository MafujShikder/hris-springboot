type HelpTopic{
    id:ID
    uid:String
    dateCreated:String
    lastUpdated:String
    name:String
    description:String
    helpChapters:[HelpChapter]
}

extend type Query{
    getHelpTopicById(id:ID) : HelpTopic

    HelpTopic(where:String, sort:String) : [HelpTopic]!
}

extend type Mutation{

}

