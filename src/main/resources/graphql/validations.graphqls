type Validation{
    id:ID
    uid:String
    dateCreated:String
    lastUpdated:String
    name:String
    description:String
    operator:String
    leftExpression:String
    rightExpression:String
}

extend type Query{
     getValidationById(id:ID) : Validation

     Validation(where:String, sort:String):[Validation]!
}

extend type Mutation{

}